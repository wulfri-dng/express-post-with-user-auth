import localStratergy from 'passport-local';
const theLocalStratergy = localStratergy.Strategy;

// const LocalStratergy = require('passport-local').Strategy;

function initialize(passport) {
//     const authenticateUser = (email, password, done) => {

//     }

//     passport.use(new theLocalStratergy({usernameField: 'email'}),
//     authenticateUser);
//     passport.serialzeUser((user, done) => {})
//     passport.deSerialzeUser((id, done) => {})
}

export default initialize;